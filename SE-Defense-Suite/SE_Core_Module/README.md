
# SE-Core (Enterprise CLI)

This is the master controller for the SE-Defense-Suite.
Run:

```
python se_core.py
```

or command mode:

```
python se_core.py --module url --file links.txt
```

You can compile to EXE using PyInstaller:

```
pyinstaller --onefile se_core.py
```
